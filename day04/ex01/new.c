// res = (a[i] - '0') + (b[i] - '0') + left;
        // if (res >= 2)
        // {
        //     res = res % 2;
        //     left = 1;
        // }
        // else
        //     left = 0;
        // a[i] = res + '0';
        // i--;